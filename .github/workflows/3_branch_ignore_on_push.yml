name: Github Action - Push - Branch Ignore Template
run-name: Running - ${{ github.event_name }}
on:
  push:
    branches-ignore:
      - main
jobs:
  on-push:
    runs-on: ubuntu-latest
    steps:
      - name: Comentario
        run: |
          echo "Comentario PUSH WORKSFLOW - branch ignore"